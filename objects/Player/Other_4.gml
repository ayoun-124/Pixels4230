// obj_player: Room Start
if (!obj_controller.gc_try_restore_player_pos(id)) {
    // No saved spot for this room yet -> use your default spawn
    // e.g., position the player at a start marker or leave as-placed
}
